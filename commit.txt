新增app\tools\pdf_exporter.py，
    save_markdown_as_pdf：用于将Markdown导出为PDF。
修改app\agents\writer.py，让 writer 输出带摘要、对比要点等结构化 Markdown。
新增api\routes\artifact.py，
    download_artifact：用FastAPI编写的后端API路由模块，定义了一个HTTP GET接口，允许用户根据task_id下载服务器上对应的PDF文件。
修改app\tools\chroma_client.py，避免空chunk导致入库报错。