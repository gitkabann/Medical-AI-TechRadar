新增app\core\async_utils.py：
    with_retry：对任意 async 函数执行：超时 + 重试 + 指数退避。
    RateLimiter：限制全局并发，例如并行最多 5 个任务。（暂时不用）
修改app\agents\pipeline_dummy.py：
    ingest_trials_safe：用asyncio.to_thread包裹同步的ingest_trials，这样可以和其他的ingest一起异步。
    ingest_all_sources：异步获取所有api信息并chunk入库
新增tests\test_async_pipeline.py，用于测试以上内容。