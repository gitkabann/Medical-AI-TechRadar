增加app\tools\rag_query.py：
    最简单的 RAG：从 Chroma 取 top_k 片段并返回 metadata。
修改app\agents\writer.py，根据 RAG 结果生成看起来更复杂的Markdown报告。
修改app\agents\pipeline_dummy.py，调用上述的query_rag和generate_markdown_report组成pipeline。
增加tests\test_rag_writer.py，测试pipeline用。