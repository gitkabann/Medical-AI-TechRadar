增加app\models\document.py,定义：
    文献/代码/试验三类文档的统一元数据 schema：DocumentMetadata (source、date、url、section)
    Chunk的schema：DocumentChunk (chunk_id、content、metadata)

增加简单的chunking.py，实现：
    按双换行或句号分段
    返回若干 DocumentChunk 对象

增加简单的chroma_client.py，实现：
    ingest()：chunk加入chroma数据库
    query()：检索问题对应的chunk